# OAuth Configuration for AunooAI
# Copy this file to your .env file and fill in the values for the providers you want to enable

# =============================================================================
# GOOGLE OAUTH CONFIGURATION
# =============================================================================
# To set up Google OAuth:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google+ API
# 4. Go to "Credentials" and create "OAuth 2.0 Client IDs"
# 5. Set authorized redirect URIs to: http://localhost:8000/auth/callback/google
#    (replace localhost:8000 with your domain in production)

GOOGLE_CLIENT_ID=316098467370-o7fck3g9ftkisddj5rj1ft9s2onc234b.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# =============================================================================
# GITHUB OAUTH CONFIGURATION  
# =============================================================================
# To set up GitHub OAuth:
# 1. Go to https://github.com/settings/applications/new
# 2. Fill in the application details
# 3. Set Authorization callback URL to: http://localhost:8000/auth/callback/github
#    (replace localhost:8000 with your domain in production)

GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# =============================================================================
# MICROSOFT OAUTH CONFIGURATION (Optional)
# =============================================================================
# To set up Microsoft OAuth:
# 1. Go to https://portal.azure.com/
# 2. Navigate to "Azure Active Directory" > "App registrations"
# 3. Create a new registration
# 4. Set redirect URI to: http://localhost:8000/auth/callback/microsoft
#    (replace localhost:8000 with your domain in production)

MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here

# =============================================================================
# OAUTH APPLICATION SETTINGS
# =============================================================================

# Where to redirect users after successful OAuth login
OAUTH_REDIRECT_AFTER_LOGIN=/

# Session timeout in seconds (default: 86400 = 24 hours)
OAUTH_SESSION_TIMEOUT=86400

# Whether to require email verification for OAuth users
OAUTH_REQUIRE_EMAIL_VERIFICATION=false

# Whether to allow new user registration via OAuth
OAUTH_ALLOW_NEW_USER_REGISTRATION=true

# Enable debug mode for OAuth (logs additional information)
OAUTH_DEBUG_MODE=false

# =============================================================================
# PRODUCTION CONFIGURATION NOTES
# =============================================================================

# For production deployment:
# 1. Use HTTPS URLs for all redirect URIs
# 2. Set appropriate CORS settings
# 3. Use secure session keys
# 4. Enable OAUTH_REQUIRE_EMAIL_VERIFICATION=true
# 5. Consider setting OAUTH_ALLOW_NEW_USER_REGISTRATION=false for private apps
# 6. Set OAUTH_DEBUG_MODE=false

# Example production redirect URIs:
# - Google: https://yourdomain.com/auth/callback/google
# - GitHub: https://yourdomain.com/auth/callback/github  
# - Microsoft: https://yourdomain.com/auth/callback/microsoft