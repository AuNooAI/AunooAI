import React from 'react'
import ReactDOM from 'react-dom/client'
import GatherApp from './GatherApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GatherApp />
  </React.StrictMode>,
)
