
  # Export to HTML and JS

  This is a code bundle for Export to HTML and JS. The original project is available at https://www.figma.com/design/KfcjAbpMlFOYkp2FYzhZ87/Export-to-HTML-and-JS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  