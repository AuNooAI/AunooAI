# Requirements for policy classifier training
# Install with: pip install -r scripts/requirements_classifier.txt

torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
scikit-learn>=1.3.0
pandas>=2.0.0
sentencepiece>=0.1.99  # Required for DeBERTa tokenizer
tiktoken>=0.5.0  # Alternative tokenizer backend

# Optional: for faster training
# bitsandbytes>=0.41.0  # For 8-bit optimizers
